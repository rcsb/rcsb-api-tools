{
  "$id": "/schema/json-schema-rcsb_search_query_facets_date_range.json",
  "title": "Date Range Facet",
  "type": "object",
  "required": [
    "aggregation_type",
    "attribute",
    "ranges"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Specifies the name of the aggregation. If undefined, the name of the attribute will be used as aggregation name"
    },
    "aggregation_type": {
      "type": "string",
      "enum": [
        "date_range"
      ],
      "description": "Specifies the type of the aggregation"
    },
    "attribute": {
      "type": "string",
      "description": "Specifies the full attribute name to aggregate on"
    },
    "ranges": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "from": {
            "$ref": "json-schema-rcsb_search_query_parameters_attribute_text_value_date_range.json#/properties/from"
          },
          "to": {
            "$ref": "json-schema-rcsb_search_query_parameters_attribute_text_value_date_range.json#/properties/to"
          }
        },
        "minProperties": 1
      },
      "minItems": 1,
      "description": "A set of ranges, each representing a bucket. Note that this aggregation includes the 'from' value and excludes the 'to' value for each range."
    },
    "min_interval_population": {
      "type": "integer",
      "description": "Minimum number of items in the bin required for the bin to be returned"
    },
    "max_num_intervals": {
      "type": "integer",
      "description": "Maximum number of intervals to return for a given facet"
    },
    "facets": {
      "$ref": "json-schema-rcsb_search_query_facets.json"
    }
  },
  "additionalProperties": false,
  "description": "This aggregation enables to define a set of ranges - each representing a bucket. Dedicated for date values with an ability to express 'from' and 'to' values as date math expression"
}
