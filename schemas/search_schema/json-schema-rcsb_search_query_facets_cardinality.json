{
  "$id": "/schema/json-schema-rcsb_search_query_facets_cardinality.json",
  "title": "Cardinality Facet",
  "type": "object",
  "required": [
    "aggregation_type",
    "attribute"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Specifies the name of the aggregation. If undefined, the name of the attribute will be used as aggregation name"
    },
    "aggregation_type": {
      "type": "string",
      "enum": [
        "cardinality"
      ],
      "description": "Specifies the type of the aggregation"
    },
    "attribute": {
      "type": "string",
      "description": "Specifies the full attribute name to aggregate on"
    }
  },
  "additionalProperties": false,
  "description": "A single-value metrics aggregation that calculates an approximate count of distinct values"
}
